spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_management
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

jwt:
  secret: your-very-long-secret-key-with-at-least-32-characters
  expiration: 86400000

logging:
  level:
    org.springframework.security: TRACE